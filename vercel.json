{
  "version": 2,
  "builds": [{ "src": "api/*.js", "use": "@vercel/node" }],
  "routes": [{ "src": "^/$", "dest": "/api/form.js" }]
}

  },
  "routes": [
    {
      "src": "^/$",
      "dest": "/api/form.js"
    }
  ]
}
